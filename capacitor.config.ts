import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ssuamkiett.bookconnect',
  appName: 'book-connect-frontend',
  webDir: 'dist/book-connect-frontend/browser'
};

export default config;
